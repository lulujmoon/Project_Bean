<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<!-- 바로 쓸 경우가 아니면 mapper는 미리 쓰지 말고 비워주세용 -->

<mapper namespace ="com.bb.bean.orders.OrdersDAO">
	<insert id="setOrdersInsert" parameterType="OrdersDTO">
		insert into orders (orderNum, id, orderName, orderDate, amount, payState, payMethod, buyerName, buyerTel, buyerPostcode, buyerAddr, buyerAddr2, message)
		values (orders_seq.nextval, #{id}, #{orderName}, sysdate, #{amount}, #{payState}, #{payMethod}, #{buyerName}, #{buyerTel}, #{buyerPostcode}, #{buyerAddr}, #{buyerAddr2}, #{message})
	</insert>
</mapper>
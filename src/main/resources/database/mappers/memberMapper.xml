<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<!-- 바로 쓸 경우가 아니면 mapper는 미리 쓰지 말고 비워주세용 -->

<mapper namespace ="com.bb.bean.member.MemberDAO">
	<insert id="memberJoin" parameterType="MemberDTO"> 
		insert into member (id, pw, name, birthday, nickname, gender, point) 
		values (#{id},#{pw},#{name},#{birthday},#{nickname},#{gender},0)
	</insert>
	
	<select id="memberLogin" parameterType="MemberDTO" resultType="MemberDTO">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	
	<update id="memberUpdate">
		update member set
	</update>
</mapper>